import * as anchor from "@coral-xyz/anchor";
import { ShadowDriveVersion } from "../types";
/**
 *
 * @param {anchor.web3.PublicKey} key - PublicKey of a Storage Account
 * @param {ShadowDriveVersion} version - ShadowDrive version (v1 or v2)
 * @returns {{ txid: string }} - Confirmed transaction ID
 */
export default function claimStake(key: anchor.web3.PublicKey, version: ShadowDriveVersion): Promise<{
    txid: string;
}>;
//# sourceMappingURL=claim-stake.d.ts.map